const initialState = {
  avaliableProduct: [], // all the products that is comming from the API or Mock
  adminProducts: [], // All the products that created by the admin (ourself)
};
export default (state = initialState, action) => {
  return state;
};
